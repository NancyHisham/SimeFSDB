from abc import ABC

class keys(ABC):
    SCHEMA = "schema"
    DB_NAME = "database_name"
    TABLES  = "Tables"
    NAME = "name"
    COLUMNS = "columns"
    PK = "primary_key"
    IDX_KEY = "Index_keys"
    CONSISTENCY = "Consistency"
